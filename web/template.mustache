<!DOCTYPE html>
<html>
    <head>
        <style>
            @import url('https://fonts.googleapis.com/css?family=Roboto&display=swap');
            @import url("https://use.fontawesome.com/releases/v5.8.1/css/all.css");

            body {
                padding: 0px;
            }

            #container {
                margin: 0px;
                font-family: 'Roboto', sans-serif;
                background: #ECF0F1;
                height: 150px;
                width: 780px;
                padding: 25px;
                border: solid 1px #ABB2B9;
                background-color: #FDFEFE;
                box-shadow: 2px 2px #ABB2B9;
                display: flex;
                flex-direction: horizontal;
            }

            #container > img {
                height: 150px;
                width: 150px;
            }

            #container > div > h1 {
                margin-top: 0px;
                margin-left: 15px;
                margin-bottom: 0px;
                color: #17202A;
                font-size: 30px;
            }

            #container > div > h2 {
                margin-top: 0px;
                margin-left: 15px;
                margin-bottom: 0px;
                color: #2C3E50;
            }

            #container > div > p {
                margin-top: 0px;
                margin-left: 15px;
                margin-bottom: 0px;
                color:#808B96;
                font-style: italic;
            }
        </style>
    </head>
    <body>
        <div id="container">
            <img src="{{ image_url }}" alt="Logo du podcast" />
            <div>
                <h1> {{ ep_title }}</h1>
                <h2> {{ pod_title }}</h2>
                <p>Cliquez pour découvrir cet épisode<p>
            </div>
        </div>
    </body>
</html>